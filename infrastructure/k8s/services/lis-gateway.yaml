---
apiVersion: v1
kind: Service
metadata:
  name: lis-gateway
  labels:
    app: lis-gateway
    app.kubernetes.io/name: lis-gateway
    app.kubernetes.io/part-of: rasteronelab-lis
spec:
  type: ClusterIP
  selector:
    app: lis-gateway
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
