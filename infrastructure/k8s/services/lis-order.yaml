---
apiVersion: v1
kind: Service
metadata:
  name: lis-order
  labels:
    app: lis-order
    app.kubernetes.io/name: lis-order
    app.kubernetes.io/part-of: rasteronelab-lis
spec:
  type: ClusterIP
  selector:
    app: lis-order
  ports:
    - name: http
      protocol: TCP
      port: 8082
      targetPort: 8082
