---
apiVersion: v1
kind: Service
metadata:
  name: lis-inventory
  labels:
    app: lis-inventory
    app.kubernetes.io/name: lis-inventory
    app.kubernetes.io/part-of: rasteronelab-lis
spec:
  type: ClusterIP
  selector:
    app: lis-inventory
  ports:
    - name: http
      protocol: TCP
      port: 8087
      targetPort: 8087
