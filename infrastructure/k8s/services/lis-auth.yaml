---
apiVersion: v1
kind: Service
metadata:
  name: lis-auth
  labels:
    app: lis-auth
    app.kubernetes.io/name: lis-auth
    app.kubernetes.io/part-of: rasteronelab-lis
spec:
  type: ClusterIP
  selector:
    app: lis-auth
  ports:
    - name: http
      protocol: TCP
      port: 8093
      targetPort: 8093
