---
apiVersion: v1
kind: Service
metadata:
  name: lis-integration
  labels:
    app: lis-integration
    app.kubernetes.io/name: lis-integration
    app.kubernetes.io/part-of: rasteronelab-lis
spec:
  type: ClusterIP
  selector:
    app: lis-integration
  ports:
    - name: http
      protocol: TCP
      port: 8092
      targetPort: 8092
