---
apiVersion: v1
kind: Service
metadata:
  name: lis-notification
  labels:
    app: lis-notification
    app.kubernetes.io/name: lis-notification
    app.kubernetes.io/part-of: rasteronelab-lis
spec:
  type: ClusterIP
  selector:
    app: lis-notification
  ports:
    - name: http
      protocol: TCP
      port: 8091
      targetPort: 8091
